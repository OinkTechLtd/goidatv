name: 🔐 Setup Dropbox Token

on:
  workflow_dispatch:
    inputs:
      dropbox_token:
        description: 'Dropbox Access Token (начинается с sl.)'
        required: true
        type: string

jobs:
  setup-token:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Save Dropbox Token
        run: |
          echo "${{ github.event.inputs.dropbox_token }}" > dropbox_token.txt
          
      - name: Commit and Push Token
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add dropbox_token.txt
          git diff --staged --quiet || git commit -m "🔐 Add Dropbox token [skip ci]"
          git push
          
      - name: Verify Token
        run: |
          echo "✅ Токен сохранен в dropbox_token.txt"
          echo "📋 Длина токена: $(wc -c < dropbox_token.txt) символов"
          echo ""
          echo "🚀 Теперь запустите workflow 'Update IPTV Release'"
